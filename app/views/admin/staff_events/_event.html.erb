<tr>
  <% unless @staff %>
    <td>
    <%= link_to(event.staff.family_name,admin_staff_staff_events_path(event.staff)) %>
    </td>
  <% end %>

  <td><%= event.description %></td>
  <td class="date">
    <%= event.occurred_at.strftime("%Y/%m/%d %H:%M:%S") %>
  </td>
</tr>